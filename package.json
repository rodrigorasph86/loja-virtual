{
  "name": "loja-virtual",
  "version": "1.0.0",
  "license": "ISC",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "serve-build": "npx lite-server --baseDir=build",
    "fix-images": "npx imagemin images-to-fix/* --out-dir=public/images"
  },
  "devDependencies": {
    "postcss": "^8.4.21",
    "postcss-preset-env": "^8.0.1",
    "sass": "^1.58.3",
    "terser": "^5.16.6",
    "vite": "^4.1.4"
  }
}
